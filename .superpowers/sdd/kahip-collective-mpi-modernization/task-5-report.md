# Task 5 report: RNG parity, MPI tracing, and projection collectives

## Scope and revisions

- Task base: `b888984371a4c038686942c22d1d8b2646b7f574`
- Behavioral oracle: pristine detached upstream worktree at
  `5935f349f65f1788a9b68fcf6d853e698d86956d`
- Implemented only Task 5: exact upstream random behavior, a default-off
  canonical MPI trace facility, and projection request/reply migration.
- Deliberately not migrated: other dense paths (Task 6), sparse ghost and
  block-propagation transports (Task 7), and evolutionary gossip.

Every command below was run under the required user systemd scope with
`MemoryHigh=28G`, `MemoryMax=30G`, and `MemorySwapMax=2G`. Builds used at most
two jobs.

## RNG oracle and TDD evidence

The literal values were generated by a standalone probe compiled directly
against the pristine upstream `random_functions.cpp`, rather than calculated
from the candidate implementation.

Seed zero produced:

- integers in `[0,1000]`: `549, 593, 715, 845, 603, 858, 545, 848`
- doubles in `[-2,3]`:
  `2.2009385857735477, -0.028085365904534809,
  1.9154961187930293, 1.9922001673803664, 2.5582367896839218,
  -1.0122431535330803, -0.3238862214255549, 1.84114797405952`
- booleans:
  `true, true, true, true, true, true, true, true, false, true, true,
  false, false, false, true, false`
- seed 17 reset tuple: integer `294665`, double
  `18.576910003822722`, boolean `false`

Initial focused RED against the branch implementation:

- integer: candidate `356`, oracle `549`
- double: candidate `-0.39601499569129328`, oracle
  `2.2009385857735477`
- boolean: candidate `false`, oracle `true`
- seed-17 integer: candidate `554417`, oracle `294665`

The first minimal restore changed boolean draws back to
`uniform_int_distribution<short>(0,1)`, doubles back to the upstream C
`rand()/RAND_MAX` affine draw, and MT seeding back to direct integer seeding.
The four focused tests then passed, but the fixed partition oracle remained
RED. Controlled builds established:

| Build | Projection | RNG state | Partition SHA256 |
|---|---|---|---|
| pristine upstream | legacy P2P | one shared global state | `a600acd0029ee9342e4f7c5b041d224a308b874c85fd35bdbcd3a5a73d48cdd0` |
| Task base + RNG draw restore | legacy P2P | split namespaced states | `eca7f7665030b8a425ad2f6d0d309329d0c245248eeb6e27b39df0e7c82a472d` |
| Task 5 projection + RNG draw restore | dense collectives | split namespaced states | `eca7f7665030b8a425ad2f6d0d309329d0c245248eeb6e27b39df0e7c82a472d` |

`nm -C` showed the root cause. Upstream linked one
`random_functions::m_mt/m_seed` symbol shared by ParHIP and modified KaHIP;
the namespace modernization had created independent `parhip::` and
`kahip::modified::` states. An alternating mixed-stream upstream probe
produced `294, false, 531, 2.2897340028033284, true`, followed by a reset
through the modified component and ParHIP draws `10,
-1.7233800723791961`.

The mixed-stream regression was RED at the second call: modified KaHIP
returned `true` where the shared upstream stream returned `false`. A neutral
`kahip::random_compat` backing state now preserves the upstream shared stream
while retaining both component namespaces. GREEN: `45 assertions in 5 test
cases`.

## Projection protocol TDD evidence

The two-rank regression owns two fine and two coarse nodes per rank. Every fine
node projects to the other rank, and fine-node order intentionally disagrees
with coarse-node/reply order. Expected fine labels are `{203,202}` on rank 0
and `{101,100}` on rank 1.

The test interposes MPI through PMPI and counts the protocol calls only while
`parallel_project` executes. RED preserved correct labels but observed:

```text
MPI_Alltoallv=1 MPI_Isend=2 MPI_Probe=2
```

This proved at runtime that the existing collective result was ignored while
the old point-to-point request/reply protocol supplied the behavior.

The implementation now performs exactly two dense `mpi::all_to_all_v`
exchanges using wire records:

- request: stable fine-global request ID plus coarse-global node ID
- reply: stable request ID, coarse-global node ID, and label

Requests are stable-sorted by coarse-global ID/request ID within destination;
replies are stable-sorted by request ID/coarse-global ID within requester.
Replies are correlated by the explicit request ID and checked against the
coarse-global ID, never by arrival position. Both segmented send buffers own
their storage through their blocking collective calls. The superseded
projection `m_messages`, `MPI_Isend`, `MPI_Probe`, and `MPI_Recv` code was
removed. GREEN preserved the exact labels and observed:

```text
MPI_Alltoallv=2 MPI_Isend=0 MPI_Probe=0
```

## Canonical MPI trace facility

`KAHIP_ENABLE_MPI_TRACE` is a target-scoped CMake option and defaults to OFF.
When OFF, the trace macro does not evaluate its argument, runtime storage is
absent, and file output is a no-op. A release run with
`KAHIP_MPI_TRACE_PATH` set wrote no trace files and retained the exact upstream
partition hash.

The readable `kahip-mpi-trace-v1` schema records:

1. graph-distribution nodes and edges
2. contraction label-to-coarse mapping
3. quotient node weights and edges
4. projection requests and replies
5. ghost label updates
6. block propagation
7. final global partition order

Canonicalization copies and sorts records by stage and then global ID,
semantic key, and payload. The exact fixture reverses insertion order and
requires identical bytes. The projection MPI fixture checks exact request and
reply text on each rank. A real two-rank block-down fixture executes the
vcycle-only hook, propagates remote block assignments, and checks exact
canonical block records.

Trace TDD evidence:

- schema/canonical test RED: trace header absent
- full smoke RED: rank trace not written before app/hook integration
- GREEN: exact canonical fixture, exact projection fixture, exact block hook,
  and the trace-enabled integration smoke all pass

The fixed `ultrafastmesh` executable is built with
`PARALLEL_LABEL_COMPRESSION`, whose CLI table omits `--num_vcycles`; its normal
one-cycle tuple therefore does not execute the vcycle-only block path. The
focused block test covers the Task 5 hook/schema. Broad algorithmic parity of
that legacy transport remains for the later block/ghost migration tasks.

## Reproducible upstream stage oracle

The pristine oracle was not modified. A separate detached worktree at the
pinned revision received only the checked-in patch
`task-5-upstream-trace.patch`. The patch is reproducible and applies cleanly:

```text
systemd-run --user --scope -p MemoryHigh=28G -p MemoryMax=30G \
  -p MemorySwapMax=2G git apply --check \
  /var/home/erich/Projects/KaHIP/.superpowers/sdd/\
kahip-collective-mpi-modernization/task-5-upstream-trace.patch
```

Patch SHA256:

```text
097676e8bebe7c411ddb6af703f394550ff41e20a4b628451ca6786c361560b0
```

Before generating an oracle trace, the instrumented upstream binary was run
with tracing OFF. Its partition remained exactly
`a600acd0029ee9342e4f7c5b041d224a308b874c85fd35bdbcd3a5a73d48cdd0`
and `cmp` against pristine returned zero, establishing scoped instrumentation
neutrality.

Both instrumented upstream and the Task 5 trace-enabled release candidate were
run with:

```text
mpiexec -n 2 parhip examples/rgg_n_2_15_s0.graph \
  --k=2 --preconfiguration=ultrafastmesh --seed=0 --save_partition
```

Both partition vectors were byte-identical and had SHA256:

```text
a600acd0029ee9342e4f7c5b041d224a308b874c85fd35bdbcd3a5a73d48cdd0
```

Upstream observes legacy projection replies at the requester, whereas the
collective candidate observes them at the owner that constructs the explicit
reply. Rank-local file hashes therefore differ legitimately. Records were
aggregated across both ranks and sorted by their complete canonical text
before comparison. Upstream and candidate were byte-identical (`cmp=0`) with
aggregate SHA256:

```text
eb5a236718b3818f68425598777d5fb49a05c59712760b71de243b38dbf61fc7
```

Exact fixed-tuple counts were:

| Stage | Records |
|---|---:|
| graph-distribution-node | 32768 |
| graph-distribution-edge | 320480 |
| contraction-label | 32768 |
| quotient-node-weight | 2898 |
| quotient-edge | 13602 |
| projection-request | 62 |
| projection-reply | 62 |
| ghost-update | 1313 |
| final-partition | 32768 |

The trace-enabled candidate was run a second time. Both rank files compared
byte-for-byte to the first run. Candidate rank hashes were
`161064e1d11a43f4e74b1c7e1b40a3d57de8e159599bf9e295a704a2733e97e6`
and
`131956602ca1266256310e66780701d2abaa441fe22921d07b94a1257651b9de`.
The compact golden manifest is checked in as `task-5-oracle-golden.txt`.

## Final verification

Trace-enabled debug configure/build/test:

```text
systemd-run --user --scope -p MemoryHigh=28G -p MemoryMax=30G \
  -p MemorySwapMax=2G cmake --preset local-gcc-debug \
  -DKAHIP_ENABLE_MPI_TRACE=ON
systemd-run --user --scope -p MemoryHigh=28G -p MemoryMax=30G \
  -p MemorySwapMax=2G cmake --build --preset build-local-gcc-debug -- -j2
systemd-run --user --scope -p MemoryHigh=28G -p MemoryMax=30G \
  -p MemorySwapMax=2G ctest --preset test-local-gcc-debug --output-on-failure
```

Result: `42/42` passed, including projection/block trace, trace integration,
and adapter ranks 1 through 5.

Default-OFF release configure/build/test:

```text
systemd-run --user --scope -p MemoryHigh=28G -p MemoryMax=30G \
  -p MemorySwapMax=2G cmake --preset local-gcc-release \
  -DKAHIP_ENABLE_MPI_TRACE=OFF
systemd-run --user --scope -p MemoryHigh=28G -p MemoryMax=30G \
  -p MemorySwapMax=2G cmake --build --preset build-local-gcc-release -- -j2
systemd-run --user --scope -p MemoryHigh=28G -p MemoryMax=30G \
  -p MemorySwapMax=2G ctest --preset test-local-gcc-release --output-on-failure
```

Result: `41/41` passed, including adapter ranks 1 through 5. The trace
integration test is intentionally registered only in trace-enabled builds.

Capability and dependency checks:

```text
#define KAHIP_HAVE_MPI_ALLTOALLV_C 0
cista_search_status=1
projection_p2p_search_status=1
```

The host correctly exercised the MPI-3 adapter path. Production Cista search
and projection P2P/member searches returned no matches. `git diff --check`
passed.

## Scope limits and concerns

- Exact parity is established only for the recorded fixed tuple and the small
  focused projection/block fixtures; this report does not claim broad
  all-configuration parity.
- Rank-local upstream/candidate trace files are not directly comparable for
  projection replies because the observable construction point changed.
  Canonical cross-rank aggregation is the semantic comparison.
- The fixed app tuple does not execute vcycle block propagation. Its hook and
  schema are exact-tested, but broader algorithmic block-path parity belongs
  with the later transport tasks.
- Existing legacy compiler warnings were retained; no warnings were
  suppressed.
